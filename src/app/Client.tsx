"use client";
import { useStoreAPI } from "../../stores/store";
import { useEffect } from "react";

type ClientProps = {};

export default function ClientManage({}: ClientProps) {
  useEffect(() => {}, []);

  return null;
}
